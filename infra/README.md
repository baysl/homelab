# Infra

This directory contains infrastructure-related components deployed in the cluster via FluxCD. Each subdirectory typically represents a specific infrastructure component or service, managed using Kustomize for configuration.
